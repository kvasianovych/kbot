replicaCount: 1
image:
  repository: kvasianovych/kbot
  pullPolicy: IfNotPresent
  tag: "1.0.3-2d2dc3a"
  arch: "amd64"
service:
  # type: ClusterIP
  type: LoadBalancer
  port: 80
secrets:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"
