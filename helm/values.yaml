replicaCount: 1
image:
  repository: kvasianovych/kbot
  pullPolicy: IfNotPresent
  registry: "ghcr.io"
  tag: "v1.0.3-7f5f9e3"
  os: linux
  arch: "amd64"
service:
  # type: ClusterIP
  type: LoadBalancer
  port: 80
secrets:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"
