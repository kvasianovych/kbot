replicaCount: 1
image:
  repository: kvasianovych/kbot
  pullPolicy: IfNotPresent
  registry: "ghcr.io"
  tag: "v1.0.3-f52936c"
  os: linux
  arch: "amd64"
service:
  # type: ClusterIP
  type: LoadBalancer
  port: 80
secrets:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"
